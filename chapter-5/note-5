第5章 数字
本章的主题是python中的数字。介绍每一种数字类型，适用的各种操作符，用于处理数字的内建函数
5.1数字简介
数字提供了标量储存和直接访问。它是不可更改类型。
python支持多种数字类型：整型、长整型、布尔型、双精度浮点型、十进制浮点型和复数
python支持的长整型类型数值仅仅与你的机器支持的（虚拟）内存大小有关，推荐用L表示长整型
real+imagej num.conjugate()共轭复数
5.6内建函数与工厂函数
python有5个运算内建函数用于数值计算：abs() coerce() divmod() pow() round()
5.6.3仅用于整型的函数
oct() 转8进制
hex() 转16进制
chr() 转字符
ord() 转数值
5.8相关模块
decimal 十进制浮点运算符
array 高效数值数组
math、cmath 标准C库数学运算函数
operator 数字操作符的函数实现
random 多种伪随机数生成器
randint()两个整型参数，返回二者之间的随机整型
randrange()接受和range()一样的参数，随机返回range([start,stop,step])结果的一项
uniform()几乎和randint()一样，不过返回的是二者之间的一个浮点型
random()返回0.0-1.0之间的浮点数
choice()随机返回给定序列的一个元素

第6章 序列：字符串、列表和元组
6.1序列
成员关系操作符 in/not in
+ * [][:][::-1][::step]
6.1.3内建函数(BIF)


